Open a terminal from this directory in any machine supports JVM
Invoke "java -jar SenateBusProblem.jar" in the terminal
It will automatically runs the program in the terminal

Note: the mean values are 30 seconds 20 minutes. It will take long to run, as per assignment.